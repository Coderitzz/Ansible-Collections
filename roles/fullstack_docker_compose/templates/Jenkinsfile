pipeline {
    agent any

    stages {
        stage('Clone Repo') {
	    steps {
                git branch: 'main', url: 'https://github.com/Coderitzz/Ansible-Collections.git'

                // Replace IP after clone
                sh '''
               sed -i "s/REPLACE-IP/{{ public_ip }}/g" app/react-app/weather_app_frontend/src/service/WeatherService.js
                '''
            }
        }

        stage('Run Docker Compose') {
            steps {
                dir('app') {
                    sh 'docker compose up -d'
                }
            }
        }
    }
}

